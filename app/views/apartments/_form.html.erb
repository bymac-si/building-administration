
<%= form_with(model: [@building, apartment], local: true, class:'p-4 p-md-5 border rounded-3 bg-light') do |form| %>
  <% if apartment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(apartment.errors.count, "error") %> prohibited
        this apartment from being saved:</h2>
      <ul>
        <% apartment.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-label">
    <%= form.label :number %>
    <%= form.text_field :number, class:'form-control' %>
  </div>
  <div class="form-label">
    <%= form.label :building %>
    <%= form.select :building_id, @buildings.map{|building|
    [building.name, building.id]}, prompt: "Seleccione una opción...", class:'form-select' %>
  </div>
  <div class="w-100 btn btn-lg btn-primary">
    <%= form.submit class:'w-100 btn btn-lg btn-primary' %>
  </div>
<% end %>
